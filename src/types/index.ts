export type HeroProps = {
  title: string;
  imagePath: string;
  tag: string;
  description: string;
  authorAvatarPath: string;
  datePublished: string;
};

export type LandingItemProps = {
  imagePath: string;
  tag: string;
  date: string;
  title: string;
};
